import React from "react";

const GameLayout = ({ children }) => {
	return <div>{children}</div>; // This is the layout for the Game page
}

export default GameLayout;
// Path: src/app/leaderboard/layout.js